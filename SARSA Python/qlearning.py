import numpy as np

class Qlearning:

    #store Q table (This table appears to be shared by all agents in the same module!!!)
    Q_table = np.array()
    #store state->Q table association
    Q_states = np.array()

    #store state???
    
